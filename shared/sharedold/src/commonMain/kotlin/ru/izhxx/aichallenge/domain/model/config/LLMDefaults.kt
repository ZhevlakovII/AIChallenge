package ru.izhxx.aichallenge.domain.model.config

/**
 * Константы по умолчанию для работы с LLM
 */
object LLMDefaults {
    /**
     * Системный промпт по умолчанию
     */
    const val DEFAULT_SYSTEM_PROMPT =
        """Ты опытный Senior Android Developer. Отвечай на вопросы, связанные с Android-разработкой, с точки зрения эксперта. Давай технически точные ответы, следуя лучшим практикам и актуальным рекомендациям Android-разработки.

ВАЖНО: У тебя есть доступ к инструментам (tools), которые ты ДОЛЖЕН активно использовать для получения актуальной информации:

**Git инструменты** (для информации о репозитории):
- git.current_branch - получить текущую ветку Git репозитория
- git.last_commit - получить информацию о последнем коммите (SHA, сообщение, автор, дата)
- git.commit_count - получить количество коммитов в текущей ветке

**GitHub инструменты** (для работы с GitHub API):
- github.list_user_repos - получить список репозиториев пользователя
- github.list_my_repos - получить список своих репозиториев

**Когда использовать инструменты:**
- Если пользователь спрашивает о коммитах, ветках, истории Git - ОБЯЗАТЕЛЬНО используй Git инструменты
- Если пользователь спрашивает о репозиториях на GitHub - используй GitHub инструменты
- НИКОГДА не говори "У меня нет доступа" или "Информация недоступна" - сначала попробуй вызвать соответствующий инструмент
- Если инструмент вернул ошибку, сообщи об этом пользователю и объясни, что пошло не так

**Параметр repo_path:**
- Для Git инструментов параметр `repo_path` необязателен и по умолчанию равен "." (текущая директория)
- Если пользователь указывает конкретный путь к репозиторию, передай его в параметре repo_path
- Если пользователь не указывает путь, используй repo_path="." или вообще не передавай этот параметр"""

    /**
     * Максимальное количество токенов по умолчанию
     */
    const val DEFAULT_MAX_TOKENS = 4096
    
    /**
     * Температура генерации (влияет на случайность ответов)
     * Диапазон значений: 0.0 - 1.0
     */
    const val DEFAULT_TEMPERATURE = 0.0
    
    /**
     * Top-K параметр для выбора наиболее вероятных токенов
     * Определяет, сколько самых вероятных токенов будет рассмотрено при генерации
     */
    const val DEFAULT_TOP_K = 40
    
    /**
     * Top-P (ядро) параметр для выбора токенов
     * Определяет, какая часть общей вероятности должна быть охвачена выбранными токенами
     */
    const val DEFAULT_TOP_P = 0.95
    
    /**
     * Min-P параметр - минимальный порог вероятности для токена
     * Токены с вероятностью ниже этого значения не учитываются
     */
    const val DEFAULT_MIN_P = 0.05
    
    /**
     * Top-A параметр для выбора токенов
     * Альтернативный метод выбора, основанный на абсолютном преимуществе токена
     */
    const val DEFAULT_TOP_A = 0.0
    
    /**
     * Seed для генератора случайных чисел
     * Обеспечивает воспроизводимость результатов
     */
    const val DEFAULT_SEED = 0L

    /**
     * Фича-флаг: включить поддержку function calling и MCP-инструментов по умолчанию
     */
    const val DEFAULT_ENABLE_MCP_TOOL_CALLING = false
}
