/*
 * Convention plugin: jvm.library
 * Базовая JVM Library конфигурация
 * Работает только для JVM модулей: для backend или desktop (JVM)
 */

plugins {
    id("org.jetbrains.kotlin.jvm")
//    id("lint")
}
